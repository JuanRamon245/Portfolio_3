import { AnimatedOnChangeHistory } from './animated-on-change-history';

describe('AnimatedOnChangeHistory', () => {
  it('should create an instance', () => {
    const directive = new AnimatedOnChangeHistory();
    expect(directive).toBeTruthy();
  });
});
